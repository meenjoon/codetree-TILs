import java.util.*
import kotlin.math.*

fun main() {
    val sc = Scanner(System.`in`)

    val a = sc.next()
    sc.nextLine()
    val b = sc.next()

    var length = a.length + b.length
    println("${length}")
}